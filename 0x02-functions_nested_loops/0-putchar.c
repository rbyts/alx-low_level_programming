#include "main.h"
/**
* main - here we describe the function but im too busy for that.
*
* Return: Always 0.
*/
int main(void)
{
char s[] = "_putchar\n";
int i;
for (i = 0; i < 9; i++)
{
_putchar(s[i]);
}
return (0);
}
