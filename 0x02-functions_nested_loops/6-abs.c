#include "main.h"
/**
* _abs - as the name of the function
*
* @n: parameter of the function
*
* Return: depending on the condition.
*/
int _abs(int n)
{
if (n < 0)
{
return (n * -1);
}
return (n);
}
